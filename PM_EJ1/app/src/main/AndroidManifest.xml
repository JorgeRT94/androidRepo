<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jorge.pm_ej1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="controller.HexagonoActivity"></activity>
        <activity android:name="controller.PentagonoActivity" />
        <activity android:name="controller.TrapecioActivity" />
        <activity android:name="controller.RectanguloActivity" />
        <activity android:name="controller.TrianguloActivity" />
        <activity android:name="controller.CirculoActivity" />
        <activity android:name="controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>